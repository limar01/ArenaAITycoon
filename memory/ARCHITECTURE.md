# ARCHITECTURE
Pointer: formal engine doc = Phase-4.4.
Confirmed: single-file HTML5 canvas (base64 assets) built by python build scripts; A* pathfinding; collision map; Ollama LLM brains (phone :11434) with scripted fallback; PWA service worker; Web Audio time-of-day BGM; MQTT remote-control worker (arenabridge_worker.py); sha-verified deploy scripts.
Constraints: never hand-edit built index.html; mobile GPU budget (S10+).
