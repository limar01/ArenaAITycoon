# ArenaAITycoon — Hermes Project Binding

Project: ArenaAITycoon (reference implementation of the Hermes indie-game studio layer).
See `AGENTS.md` for the agent protocol; `docs/HERMES_STUDIO_ARCHITECTURE.md` for the full architecture.

## Runtime bindings
- Profiles: machine-level Hermes profiles exist for all personas (`~/.hermes/profiles/`: zillion, chronos, marcus, aria, cody, pixel, echo, vortex, jax, vanguard). Project role cards: `studio/agents/*.md` (source of truth for responsibilities; profiles own voice/model).
- Skills: project skills in `.hermes/skills/` (game-studio bundle + 12). Global Hermes skills (`github/*`, `python-project-qa`, `software-development/*`) are REUSED, not duplicated.
- Workflows: `studio/workflows/` (production_pipeline, gauntlet_loop, vertical_slice_qa, restore).

## Model tiers (capability-based; concrete IDs resolved at runtime)
- STRONG: director + independent review + hard architecture (Zillion, Vanguard).
- CODING: implementation/debug (Cody, Vortex).
- FAST: routine docs/memory/simple specs (Marcus, Aria, Pixel, Echo, Chronos maintenance).
Observed providers (2026-09-22): Hermes default `nous` (upstage/solar-pro4:free); Ollama phone `llama3.2:1b|qwen2.5:1.5b|qwen2.5-coder:1.5b|deepseek-r1:1.5b`; one unidentified custom CF `/v1` provider = DO NOT USE until identified.

## Memory
Canonical structured set: `memory/` (11 files, schema in architecture doc §3). Legacy brain `memory/MEMORY_CORE.md` = append-only archive + pointer. Single writer for `CURRENT_SPRINT.md` = Chronos.

## Gates
G0–G8 (architecture §4). Current: G5 Alpha. Boss = sole approver. Never auto-approve.

## Hard rules (studio-level)
- Audit-before-modify; additive-only changes until Boss review of the migration plan.
- No hand edits to `builds/**/index.html`; changes via build scripts + sha-verified deploy.
- Evidence over claims (Jax schema); Vanguard independent; ≤3 gauntlet cycles then escalate.
- Git: small commits, no destructive ops without Boss approval, no credentials in remotes.
- Reporting language: English (doctrine v1.3.9); Taglish = in-game flavor only (pending Boss confirmation, audit §7.2).
